-- Add IS_INTERNAL flag to SS_MESSAGES
-- Run against HLDEV and HLPRD before deploying messages.php
ALTER TABLE SS_MESSAGES
    ADD COLUMN IS_INTERNAL TINYINT(1) NOT NULL DEFAULT 0 AFTER MESSAGE_BODY;
